# YHK-Cat-Thermal-Printer
## How to run
```bash
python3 print_gui.py
```

- *NOTE: Currently multiple images are not supported. Although user can select multiple images, only the last image will be printed.*